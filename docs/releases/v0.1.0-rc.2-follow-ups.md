# Codex Friendly / Codex Flow v0.1.0-rc.2 Follow-ups

These are closed-alpha follow-up items. Do not implement them as part of the distribution-pack documentation task.

## Low hardening and evidence follow-ups

- Harden JSON reads to tolerate UTF-8 BOM in `.codexflow/state.json`.
- Add clearer dashboard recovery when state JSON parse fails.
- Run real installed-VSIX Codex CLI E2E without stub.
- Capture stronger screenshot/output evidence for final release.

## Alpha validation target

Run one small real Codex CLI phase from the installed VSIX without a mock or stub. The phase should write only to `docs/handoff/alpha-real-codex-cli.md`, keep logs under `.codexflow/logs/**`, and avoid source-file changes unless explicitly intended.
